<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.activity.FeedActivity">

    <include layout="@layout/app_toolbar" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycler_feed"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/appToolbar"
        android:scrollbars="none"/>

    <ImageButton
        android:id="@+id/fabCreate"
        android:layout_width="@dimen/btnFabSize"
        android:layout_height="@dimen/btnFabSize"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="@dimen/btnFabMargins"
        android:layout_marginRight="@dimen/btnFabMargins"
        android:elevation="@dimen/defaultElevation"
        android:textSize="28sp"
        android:src="@drawable/ic_instagram_white"
        android:background="@drawable/btn_fab_default"/>


</RelativeLayout>
